module.exports = {
  // Точки с запятой в конце
  semi: false,
  // Завершающие запятые
  trailingComma: 'none',
  // Одинарные кавычки
  singleQuote: true,
  // Длина строки, по которой приттер будет выполнять перенос
  printWidth: 80,
  // Табы
  tabWidth: 2,
  // "always"- Всегда включать скобки. Пример:(x) => x
  // "avoid"- Опускать скобки, когда это возможно. Пример:x => x
  arrowParens: 'avoid',
  // Добавление последней строки
  endOfLine: 'lf',
  // Печатать пробелы между скобками в объектаx
  bracketSpacing: true
}
